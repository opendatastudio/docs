---
title: Using custom Docker containers
description: Using custom Docker containers with datapackages
---

import { FileTree } from "@astrojs/starlight/components";
import { Aside } from "@astrojs/starlight/components";

You can use your own custom Docker containers to execute algorithms in.

By default, all algorithms are set to run in opendata.studio's default [python-run-base]() Docker container.
